import 'package:flutter/material.dart';

class AppConstants {
  static const String appName = 'LuckyFit 90';
  static const String tagline = 'Transform Yourself in 90 Days';
  static const Color gold = Color(0xFFD4AF37);
  static const Color accent = Color(0xFFFFD54F);
  static const Color background = Color(0xFF0D0D0D);
  static const Color card = Color(0xFF1A1A1A);
  static const double radius = 22;
  static const List<String> quotes = [
    'Small daily wins create a 90-day transformation.',
    'Eat strong, lift smart, sleep deep.',
    'Consistency beats intensity when motivation fades.',
    'Your future body is built by today\'s choices.',
  ];
}
