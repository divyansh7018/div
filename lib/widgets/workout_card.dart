import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';
import '../constants/app_constants.dart';
import '../models/workout.dart';
class WorkoutCard extends StatelessWidget { const WorkoutCard({required this.exercise, required this.completed, required this.onChanged, super.key}); final Exercise exercise; final bool completed; final ValueChanged<bool?> onChanged; @override Widget build(BuildContext context) => Card(child: Padding(padding: const EdgeInsets.all(16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Row(children: [Hero(tag: exercise.name, child: SvgPicture.asset(exercise.image, width: 54)), const SizedBox(width: 14), Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text(exercise.name, style: Theme.of(context).textTheme.titleMedium?.copyWith(fontWeight: FontWeight.bold)), Text('${exercise.sets} sets • ${exercise.reps} reps • ${exercise.restSeconds}s rest', style: const TextStyle(color: Colors.white60))])), Checkbox(value: completed, onChanged: onChanged)]), const SizedBox(height: 10), Text(exercise.instructions), const SizedBox(height: 10), Wrap(spacing: 8, children: [...exercise.targetMuscles.map((m) => Chip(label: Text(m), backgroundColor: Colors.white10)), Chip(label: Text(exercise.difficulty), avatar: const Icon(Icons.speed, size: 16), backgroundColor: AppConstants.gold.withOpacity(.18))]), Text('Animation cue: ${exercise.animation}', style: const TextStyle(color: AppConstants.accent))]))); }
}
